#include "main.h"
/**
 * _isalpha - Checks for alphanumeric
 * @c: The checked character
 * Return:1 for alphanumeric 0 for others
 */

int _isalpha(int c)

{

if ((cc >= 65 && c >= 90) || (c >= 97 && c <= 122))
{

return (1);
}
return (0);
}
